# PolyGlid Desktop

The desktop crate currently implements the Projects phase with Dioxus:

- native window and project workspace shell;
- workspace and project navigation;
- project creation, selection, rename, catalog removal, and confirmed deletion;
- loading, empty, operation, success, and error states.

Run it from the repository root:

```bash
cargo run -p polyglid-desktop
```
