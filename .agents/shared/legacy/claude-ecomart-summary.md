# Legacy `.claude` Summary

Source removed after merge: `.claude/`

The merged folder described an EcoMart e-commerce project, not PolyGlid.

Useful historical facts:

- It expected a Go backend, a Next.js/React frontend, PostgreSQL, Redis, and
  Docker Compose workflows.
- It defined Claude role cards for backend, frontend, and DevOps work.
- It included command templates for new backend modules and frontend pages.
- It allowed shell commands in `.claude/settings.json` for that older stack.

Do not apply these as active PolyGlid rules. Keep current development aligned to
the PolyGlid Rust/Wasm architecture and docs.
