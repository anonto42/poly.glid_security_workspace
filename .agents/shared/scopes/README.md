# Scope Index

Load only the scope that matches the task:

- Dioxus window, shell, styles, state, or Projects UI: `desktop.md`
- Client gateway, controllers, core services, stores, security, runtime, or
  WIT contracts: `domain-core.md`

For changes spanning both areas, load both scopes and
`../context/architecture.md`.
